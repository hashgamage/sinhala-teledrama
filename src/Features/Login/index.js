import React,{PureComponents} from 'react'
import {
    View,
    StyleSheet
} from 'react-native'
import color from '../config/color';

class Login extends PureComponents{
    render(){
        return(
            <View>Home</View>
        )
    }
}

const styles = StyleSheet.create({
    container:{
        
    }
})

export default Login